<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wattar Academy Management</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

    <style>
        .sidebar {
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .sidebar .nav-link {
            color: rgba(255,255,255,0.8);
            padding: 15px 20px;
            border-radius: 8px;
            margin: 5px 10px;
            transition: all 0.3s;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            background: rgba(255,255,255,0.2);
            color: white;
        }

        .main-content {
            background-color: #f8f9fa;
            min-height: 100vh;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
        }

        .user-info {
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            padding: 15px;
            margin: 10px;
            color: white;
        }

        .role-badge {
            font-size: 0.8em;
            padding: 4px 8px;
            border-radius: 12px;
            background: rgba(255,255,255,0.2);
        }

        @media (max-width: 991.98px) {
            .sidebar {
                min-height: auto;
            }
        }
    </style>
</head>

<body>

<div class="container-fluid">
    <div class="row">

        <!-- Mobile Navbar -->
        <nav class="navbar navbar-light bg-white shadow-sm d-lg-none">
            <div class="container-fluid">
                <button class="btn btn-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar">
                    <i class="fas fa-bars"></i>
                </button>
                <span class="navbar-brand mb-0 h5">
                    <i class="fas fa-music me-2"></i>Wattar Academy
                </span>
            </div>
        </nav>

        <!-- Desktop Sidebar -->
        <div class="col-lg-2 d-none d-lg-block sidebar">
            <div>

                <h4 class="text-white mb-4 mt-4">
                    <i class="fas fa-music me-2"></i>
                    Wattar Academy
                </h4>

                <% if (typeof user !== 'undefined') { %>
                <div class="user-info mb-4">
                    <div class="d-flex align-items-center mb-2">
                        <i class="fas fa-user-circle fa-2x me-2"></i>
                        <div>
                            <div class="fw-bold"><%= user.full_name %></div>
                            <span class="role-badge">
                                <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>
                            </span>
                        </div>
                    </div>
                </div>
                <% } %>

                <nav class="nav flex-column">

                    <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                    <a class="nav-link  <%= currentUrl == '/dashboard' ? 'active' : '' %>" href="/">
                        <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                    </a>
                    <% } %>
                    
                    <% if (typeof user !== 'undefined' && ['operations_manager', 'manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/session-confirmations' ? 'active' : '' %>" href="/session-confirmations">
                        <i class="fas fa-phone-alt me-2"></i>Session Confirmations
                    </a>
                    <a class="nav-link <%= currentUrl == '/pre-schedule' ? 'active' : '' %>" href="/pre-schedule">
                        <i class="fas fa-calendar-week me-2"></i>Weekly Schedule
                    </a>
                    <% } %>
                    
                    <% if (typeof user !== 'undefined' && ['reception', 'trainer'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/pre-schedule' ? 'active' : '' %>" href="/pre-schedule">
                        <i class="fas fa-calendar-week me-2"></i>Weekly Schedule
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['trainer', 'manager', 'reception', 'operations_manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/evaluations' ? 'active' : '' %>" href="/evaluations">
                        <i class="fas fa-star me-2"></i>Students Evaluation
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && user.role !== 'sales' && user.role !== 'trainer') { %>
                    <a class="nav-link <%= currentUrl == '/students' ? 'active' : '' %>" href="/students">
                        <i class="fas fa-users me-2"></i>Students
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/classes' ? 'active' : '' %>" href="/classes">
                        <i class="fas fa-chalkboard-teacher me-2"></i>Classes
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager', 'reception'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/attendance' ? 'active' : '' %>" href="/attendance">
                        <i class="fas fa-calendar-check me-2"></i>Attendance
                    </a>
                    <% } %>
                    
                    <% if (typeof user !== 'undefined' && user.role === 'operations_manager') { %>
                    <a class="nav-link <%= currentUrl == '/attendance' ? 'active' : '' %>" href="/attendance">
                        <i class="fas fa-calendar-check me-2"></i>Attendance (View Only)
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/attendance/summary' ? 'active' : '' %>" href="/attendance/summary">
                        <i class="fas fa-chart-line me-2"></i>Session Summary
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/reports' ? 'active' : '' %>" href="/reports">
                        <i class="fas fa-chart-bar me-2"></i>Reports
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager', 'reception'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/cash' ? 'active' : '' %>" href="/cash">
                        <i class="fas fa-money-bill-wave me-2"></i>Cash Management
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['sales', 'manager', 'operations_manager', 'reception'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/leads' ? 'active' : '' %>" href="/leads">
                        <i class="fas fa-headset me-2"></i>Leads
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager', 'operations_manager', 'reception'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/band' ? 'active' : '' %>" href="/band">
                        <i class="fas fa-guitar me-2"></i>Wattar Band
                    </a>
                    <% } %>

                    <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                    <a class="nav-link <%= currentUrl == '/users' ? 'active' : '' %>" href="/users">
                        <i class="fas fa-user-cog me-2"></i>User Management
                    </a>
                    <a class="nav-link <%= currentUrl == '/admin/db' ? 'active' : '' %>" href="/admin/db">
                        <i class="fas fa-database me-2"></i>Database Admin
                    </a>
                    <% } %>

                    <hr class="text-white-50">

                    <a class="nav-link" href="/logout">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                    </a>

                </nav>
            </div>
        </div>

        <!-- Mobile Sidebar (Offcanvas) -->
        <div class="offcanvas offcanvas-start sidebar text-white" tabindex="-1" id="mobileSidebar">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title">
                    <i class="fas fa-music me-2"></i>Wattar Academy
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
            </div>

            <div class="offcanvas-body p-0">
                <div>

                    <% if (typeof user !== 'undefined') { %>
                    <div class="user-info mb-4">
                        <div class="d-flex align-items-center mb-2">
                            <i class="fas fa-user-circle fa-2x me-2"></i>
                            <div>
                                <div class="fw-bold"><%= user.full_name %></div>
                                <span class="role-badge">
                                    <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>
                                </span>
                            </div>
                        </div>
                    </div>
                    <% } %>

                    <nav class="nav flex-column">

                        <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/dashboard' ? 'active' : '' %>"" href="/">Dashboard</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['operations_manager', 'manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/session-confirmations' ? 'active' : '' %>" href="/session-confirmations">Session Confirmations</a>
                        <a class="nav-link text-white <%= currentUrl == '/pre-schedule' ? 'active' : '' %>" href="/pre-schedule">Weekly Schedule</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['reception', 'trainer'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/pre-schedule' ? 'active' : '' %>" href="/pre-schedule">Weekly Schedule</a>
                        <% } %>

                        <% if (typeof user !== 'undefined' && ['trainer', 'manager', 'reception', 'operations_manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/evaluations' ? 'active' : '' %>" href="/evaluations">Students Evaluation</a>
                        <% } %>

                        <% if (typeof user !== 'undefined' && user.role !== 'sales' && user.role !== 'trainer') { %>
                        <a class="nav-link text-white <%= currentUrl == '/students' ? 'active' : '' %>"" href="/students">Students</a>
                        <% } %>

                        <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/classes' ? 'active' : '' %>"" href="/classes">Classes</a>
                        <% } %>

                        <% if (typeof user !== 'undefined' && ['manager', 'reception'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/attendance' ? 'active' : '' %>"" href="/attendance">Attendance</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && user.role === 'operations_manager') { %>
                        <a class="nav-link text-white <%= currentUrl == '/attendance' ? 'active' : '' %>"" href="/attendance">Attendance (View Only)</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/attendance/summary' ? 'active' : '' %>"" href="/attendance/summary">Session Summary</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/reports' ? 'active' : '' %>"" href="/reports">Reports</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['manager', 'reception'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/cash' ? 'active' : '' %>"" href="/cash">Cash Management</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['sales', 'manager', 'operations_manager', 'reception'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/leads' ? 'active' : '' %>" href="/leads">Leads</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['manager', 'operations_manager', 'reception'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/band' ? 'active' : '' %>" href="/band">Wattar Band</a>
                        <% } %>
                        
                        <% if (typeof user !== 'undefined' && ['manager'].includes(user.role)) { %>
                        <a class="nav-link text-white <%= currentUrl == '/users' ? 'active' : '' %>"" href="/users">User Management</a>
                        <% } %>

                        <hr class="text-white-50">

                        <a class="nav-link text-white" href="/logout">Logout</a>

                    </nav>

                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="col-lg-10 col-12 main-content">
            <div class="p-4">
                <%- body %>
            </div>
        </div>

    </div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/students.js"></script>

</body>
</html>
